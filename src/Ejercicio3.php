<?php

namespace Eressea\Ejercicios;

class Ejercicio3
{

  /**
   * Un correo electrónico válido consiste de un nombre local y un nombre de dominio,
   * separado por el caracter "@". El correo electrónico puede contener letras minúsculas,
   * además de los caracteres "." o "+".
   * Dado un array de cadenas de correo electrónico "emails",
   * donde se enviará un correo a cada elemento en "emails" devolver el número de correos válidos enviados.
   * @param array $emails
   * @return int
   */
  public function emailsEnviados(array $emails): int
  {
    $result = 0;
    return $result;
  }
}
